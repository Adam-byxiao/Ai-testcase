# 现状分析记录

## 关键问题
1) 视觉节点 label 明显错误：
- 例如 "0:11" 被截断为 "011"，导致节点命名失真。
- 计时/小字被当成主节点标题。

2) JSON 边未正确过滤：
- flow_banner_flowcharts 中出现全局 JSON 边，说明未按分组/映射投影。

3) 视觉节点与 JSON 节点映射不稳定：
- 大量 0.0 匹配分数。
- LLM 映射有 JSON 解析失败（error: '\n  "mappings"').

## 结论
当前策略结构化输出有价值，但节点语义与边投影不可靠，难以作为最终流程识别结果。

## 下一步建议（旧策略）
- 修复视觉节点 label 规则（过滤纯数字/时间）。
- 按映射表投影 JSON 边，剔除全局边。
- 强化 LLM 映射 JSON 清洗。
